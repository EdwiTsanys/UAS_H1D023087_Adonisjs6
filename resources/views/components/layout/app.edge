<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title || 'Inventaris Barang' }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      min-height: 100vh;
      display: flex;
    }
    .sidebar {
      width: 240px;
      background-color: #f8f9fa;
    }
    .content {
      flex: 1;
      padding: 20px;
    }
    .nav-link.active {
      font-weight: bold;
    }
  </style>
  @if ($slots.meta)
    {{{ await $slots.meta() }}}
  @endif
</head>
<body>
  <!-- Sidebar -->
  <div class="sidebar p-3 border-end">
    <h4 class="mb-4">Inventaris</h4>
    <ul class="nav flex-column">
      <li class="nav-item">
        <a href="/barang" class="nav-link">Barang</a>
      </li>
      <li class="nav-item">
        <a href="/lokasi" class="nav-link">Lokasi</a>
      </li>
      <li class="nav-item">
        <a href="/mutasi" class="nav-link">Mutasi</a>
      </li>
      <li class="nav-item">
        <a href="/penghapusan" class="nav-link">Penghapusan</a>
      </li>
      <li class="nav-item">
        <a href="/laporan" class="nav-link">Laporan</a>
      </li>
    </ul>
    <form action="/logout" method="POST">
      <button type="submit" class="btn btn-outline-danger w-100">Logout</button>
    </form>
  </div>

  <!-- Main Content -->
  <div class="content">
    {{{ await $slots.main() }}}
  </div>
</body>
</html>
